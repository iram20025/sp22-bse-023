pipeline {
    agent any
    stages {
        stage("Check Files") {
            steps {
                sh "ls -l"
            }
        }
        stage("Run Python") {
            steps {
                sh "python3 hello.py"
            }
        }
    }
}
